---
---

<div id="cart-drawer-overlay" class="cart-overlay"></div>
<div id="cart-drawer" class="cart-drawer">
    <div class="cart-header">
        <h3>YOUR SELECTION</h3>
        <button id="close-cart" class="close-btn">&times;</button>
    </div>
    
    <div id="cart-items" class="cart-items-container">
        <!-- Items will be injected here via client-side script -->
        <p class="empty-msg">Your ritual is currently empty.</p>
    </div>

    <div class="cart-footer">
        <div class="cart-total">
            <span>SUBTOTAL</span>
            <span id="cart-subtotal">$0.00</span>
        </div>
        <button class="checkout-btn">PROCEED TO CHECKOUT</button>
        <p class="shipping-note">Complimentary shipping on orders over $250.</p>
    </div>
</div>

<style>
    .cart-overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.8);
        backdrop-filter: blur(4px);
        opacity: 0;
        visibility: hidden;
        transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
        z-index: 1000;
    }

    .cart-overlay.open {
        opacity: 1;
        visibility: visible;
    }

    .cart-drawer {
        position: fixed;
        top: 0;
        right: -450px;
        width: 100%;
        max-width: 450px;
        height: 100%;
        background: #0A0A0A;
        border-left: 1px solid #3A3A3A;
        box-shadow: -10px 0 30px rgba(0, 0, 0, 0.5);
        transition: right 0.4s cubic-bezier(0.16, 1, 0.3, 1);
        z-index: 1001;
        display: flex;
        flex-direction: column;
    }

    .cart-drawer.open {
        right: 0;
    }

    .cart-header {
        padding: 2rem;
        border-bottom: 1px solid #1A1A1A;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .cart-header h3 {
        font-family: 'Outfit', sans-serif;
        font-weight: 300;
        letter-spacing: 0.2rem;
        margin: 0;
        font-size: 1.2rem;
    }

    .close-btn {
        background: none;
        border: none;
        color: #888;
        font-size: 2rem;
        cursor: pointer;
        transition: color 0.3s;
    }

    .close-btn:hover {
        color: #FFF;
    }

    .cart-items-container {
        flex: 1;
        overflow-y: auto;
        padding: 2rem;
    }

    .empty-msg {
        color: #666;
        text-align: center;
        margin-top: 4rem;
        font-style: italic;
    }

    .cart-item {
        display: flex;
        gap: 1.5rem;
        margin-bottom: 2rem;
        padding-bottom: 1.5rem;
        border-bottom: 1px solid #1A1A1A;
    }

    .item-info {
        flex: 1;
    }

    .item-name {
        display: block;
        font-family: 'Playfair Display', serif;
        font-size: 1.1rem;
        margin-bottom: 0.25rem;
    }

    .item-price {
        color: #D4AF37;
        font-family: 'Outfit', sans-serif;
        font-size: 0.9rem;
    }

    .item-controls {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-top: 1rem;
    }

    .qty-selector {
        display: flex;
        align-items: center;
        background: #111;
        border: 1px solid #222;
        border-radius: 2px;
    }

    .qty-btn {
        background: none;
        border: none;
        color: #888;
        width: 32px;
        height: 32px;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        font-size: 1rem;
        transition: color 0.2s;
    }

    .qty-btn:hover {
        color: #FFF;
    }

    .item-qty {
        font-family: 'Outfit', sans-serif;
        font-size: 0.9rem;
        min-width: 20px;
        text-align: center;
    }

    .remove-btn {
        background: none;
        border: none;
        color: #888;
        font-size: 0.75rem;
        letter-spacing: 0.05rem;
        cursor: pointer;
        text-decoration: underline;
    }

    .item-img-placeholder {
        width: 80px;
        height: 80px;
        background: linear-gradient(135deg, #1a1a1a, #0a0a0a);
        border: 1px solid #333;
        border-radius: 4px;
        position: relative;
        flex-shrink: 0;
    }

    .item-img-placeholder::after {
        content: '';
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 20px;
        height: 20px;
        background: #D4AF37;
        clip-path: polygon(50% 0%, 0% 100%, 100% 100%);
        opacity: 0.3;
    }

    .cart-footer {
        padding: 2rem;
        background: #0D0D0D;
        border-top: 1px solid #1A1A1A;
    }

    .cart-total {
        display: flex;
        justify-content: space-between;
        margin-bottom: 1.5rem;
        font-family: 'Outfit', sans-serif;
        letter-spacing: 0.1rem;
    }

    .checkout-btn {
        width: 100%;
        background: #FFF;
        color: #000;
        border: none;
        padding: 1.2rem;
        font-family: 'Outfit', sans-serif;
        font-weight: 600;
        letter-spacing: 0.1rem;
        cursor: pointer;
        transition: all 0.3s;
    }

    .checkout-btn:hover {
        background: #D4AF37;
    }

    .shipping-note {
        font-size: 0.7rem;
        color: #666;
        text-align: center;
        margin-top: 1rem;
    }
</style>
